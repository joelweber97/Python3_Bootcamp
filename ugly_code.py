import math
import sys;


def example1():
    # This is a long comment. This should be wrapped to fit within 72
    # characters.
    some_tupe = (1, 2, 3, 'a');
    some_variable = {'long': 'Long code lines should be wrapped within 79 characters.', 'other': [math.pi, 100, 200, 300, 9876543210, 'This is a long string that goes on', 'more': {'inner': 'This while logical ine should be wrapped.', some_tuple: [1, 20, 300, 40000, 5000000000, 60000000000000000]}}
    is_cat_owner == True
    if is_cat_owner == True:
        print('meow')
    return (some_tuple, some_variable)

def example2(): return {'has_key() is deprecated': True}.has_key(
    {'f': 2}.has_key(''));

class Example3(object):
    def __init__(self, bar):
    if bar: bar += 1; bar = bar * bar; return bar
    else:
                some_string = """
                    inentation in multiline strings should not
            be touched. Only actual
                    code with be indented.
                    """
                    return (sys.path, some_string)
